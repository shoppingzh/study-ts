namespace Variable {
  enum Mode {
    Dark,
    Light
  }
  
  let a: number = 1
  const str: string = 'hello'
  const bool: boolean = true
  const arr: Array<number> = [1, 2, 3]
  const mode: Mode = Mode.Dark
}